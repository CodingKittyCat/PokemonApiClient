<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ApiClient.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ApiClient.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBox x:Name="SearchBox"
                 PlaceholderText="Search by name..."
                 TextChanged="SearchBox_TextChanged"/>
        <TextBlock Text="Pokémon List" FontSize="24" FontWeight="Bold" Margin="10" />
        <ListView
            x:Name="PokemonList"
            Margin="10"
            Width="300"
            Height="500">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:PokemonResult">
                    <StackPanel Orientation="Vertical" Margin="5">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                        <TextBlock Text="{Binding Url}" FontStyle="Italic" Margin="0,0,0,5"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Window>
